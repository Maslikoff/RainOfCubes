public class CubeStatisticsDisplay : StatisticsDisplay<Cube>
{
    protected override void UpdateDisplay(int totalSpawned, int totalCreated, int activeCount)
    {
        base.UpdateDisplay(totalSpawned, totalCreated, activeCount);
    }
}